[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd python-api && pip install -r requirements.txt"]

[start]
cmd = "cd python-api && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
